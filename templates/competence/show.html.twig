{% extends 'base.html.twig' %}

{% block title %}Compétence | Seuc Quizz{% endblock %}

{% block body %}
    <h1>Détails de la Compétence</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ competence.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ competence.title }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('competence_index', {'article': article, 'parent': parent}) }}">Retour à la liste</a> ||

    <a href="{{ path('competence_edit', {'id': competence.id, 'article': article, 'parent': parent}) }}">Modifier</a>

    {{ include('competence/_delete_form.html.twig') }}
{% endblock %}
