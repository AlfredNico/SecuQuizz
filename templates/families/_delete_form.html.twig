<form method="post" action="{{ path('families_delete', {'id': family.id, 'min': min}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ family.id) }}">
    <button class="btn"><i class="mdi mdi-close"></i>Supprimer</button>
</form>
