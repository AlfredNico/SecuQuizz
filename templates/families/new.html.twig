{% extends 'base.html.twig' %}

{% block title %}New Families{% endblock %}

{% block body %}
    <h1>Create new Families</h1>

    {{ include('families/_form.html.twig') }}

    {# <a href="{{ path('families_niveau', {'id': family.parent.id}) }}">back to list</a> #}
{% endblock %}
