<form method="post" action="{{ path('niveau_delete', {'id': niveau.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ niveau.id) }}">
    <button class="btn btn-warning"> <i class="mdi mdi-close" aria-hidden="true"></i><span>  Suppimer</span></button>
</form>
