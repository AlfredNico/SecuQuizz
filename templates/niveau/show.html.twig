{% extends 'base.html.twig' %}

{% block title %}Détail Niveau{% endblock %}

{% block body %}
    <h1>Niveau</h1>
    <div
        style="position: absolute; right: 0; margin-top: -50px; margin-right: 20px;">
        <a href="{{ path('niveau_edit', {'id': id}) }}" class="btn btn-info">
            <i class="mdi mdi-pencil" aria-hidden="true"></i>
            <span>
                Editer</span></a>
        <a href="{{ path('niveau_index') }}" class="btn btn-success">
            <i class="fa fa-undo" aria-hidden="true"></i>
            <span>
                retour niveau</span></a>
    </div>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ niveau.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ niveau.title }}</td>
            </tr>
            <tr>
                <th>Ordre</th>
                <td>{{ niveau.ordre }}</td>
            </tr>
        </tbody>
    </table>
    {% set articles = FamiliesRepository.findAll() %}
    {% if articles == null %}
        {{ include('niveau/_delete_form.html.twig') }}
    {% endif %}
{% endblock %}