{% extends 'base.html.twig' %}

{% block title %}Niveau index{% endblock %}

{% block body %}
    <h1>Niveau index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Title</th>
                <th>Ordre</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for niveau in niveau %}
            <tr>
                <td>{{ niveau.id }}</td>
                <td>{{ niveau.title }}</td>
                <td>{{ niveau.ordre }}</td>
                <td>
                    <a href="{{ path('niveau_show', {'id': niveau.id}) }}">show</a>
                    <a href="{{ path('niveau_edit', {'id': niveau.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('niveau_new') }}">Create new</a>
{% endblock %}
