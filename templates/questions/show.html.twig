{% extends 'base.html.twig' %}

{% block title %}Questions{% endblock %}

{% block body %}
    <h1>Questions</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ question.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ question.title }}</td>
            </tr>
            <tr>
                <th>Attached</th>
                <td>{{ question.attached }}</td>
            </tr>
            <tr>
                <th>TexteComplementaire</th>
                <td>{{ question.texteComplementaire }}</td>
            </tr>
            <tr>
                <th>AutreTexte</th>
                <td>{{ question.autreTexte }}</td>
            </tr>
            <tr>
                <th>Etat</th>
                <td>{{ question.etat }}</td>
            </tr>
            <tr>
                <th>Motif</th>
                <td>{{ question.motif }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('questions_index') }}">back to list</a>

    <a href="{{ path('questions_edit', {'id': question.id}) }}">edit</a>

    {{ include('questions/_delete_form.html.twig') }}
{% endblock %}
